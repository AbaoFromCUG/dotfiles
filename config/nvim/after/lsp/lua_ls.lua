---@diagnostic disable: missing-fields
---@type lspconfig.options.lua_ls
return {
    settings = {
        Lua = {
            hint = {
                enable = true,
            },
        },
    },
}
