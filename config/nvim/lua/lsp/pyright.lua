return function(config)
    config.settings = {
        python = {
            -- formatting = {
            --     provider = "autopep8"
            -- },
            -- linting = {
            --     enabled = true,
            --     pylintEnabled = true,
            -- }
        }
    }
end
